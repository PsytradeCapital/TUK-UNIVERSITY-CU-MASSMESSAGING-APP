name: tuk_cu_mass_messaging_app
description: TUK CU Mass Messaging App - Technical University of Kenya Christian Union mass messaging and attendance system. "Raising to Serve"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Database
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # SMS functionality
  telephony: ^0.2.0
  
  # State management
  provider: ^6.1.1
  
  # Security and encryption
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.1.6
  
  # File system access
  path_provider: ^2.1.1
  
  # CSV export and sharing
  csv: ^5.0.2
  share_plus: ^7.2.1
  
  # UI
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/cu_logo_new_dark.png
    - assets/cu_logo_new_light.png